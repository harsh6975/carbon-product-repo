import { HeaderMenuButton } from "@carbon/react";
import React from "react";

const StatCharts = (props) => {
  return <HeaderMenuButton ariaLabel="Stat" active={"isSideNavExpanded"} />;
};

export default StatCharts;
